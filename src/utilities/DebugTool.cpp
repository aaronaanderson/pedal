#include "pedal/DebugTool.hpp"

/*
void DebugTool::update(int currentFrame){
    //update flags based on buffer position
    if(currentFrame == 0){
      firstFrameFlag = true;
      lastFrameFlag = false;
    }else if(currentFrame == pdlSettings::bufferSize - 1){
      firstFrameFlag = false;
      lastFrameFlag = true;
    }else{
      firstFrameFlag = lastFrameFlag = false;
    }
}
*/